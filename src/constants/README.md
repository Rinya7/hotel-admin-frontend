# Константы приложения

## 📁 defaults.ts - Значения по умолчанию

**⚠️ ВАЖНО:** Этот файл содержит все значения по умолчанию для приложения. 
Здесь можно легко изменить настройки по умолчанию для всех форм и компонентов.

---

## 📍 Расположение

**Frontend:** `admin-frontend/src/constants/defaults.ts`  
**Backend:** `hotel-backend/src/constants/defaults.ts`

---

## 📋 Доступные константы (Frontend):

| Константа | Описание | Текущее значение |
|-----------|----------|-------------------|
| `DEFAULT_HOTEL_LOGO_URL` | Заглушка логотипа отеля (если logo_url не указан) | `https://img.icons8.com/emoji/96/hotel-emoji.png` |
| `DEFAULT_PHONE_COUNTRY_CODE` | Код страны по умолчанию для телефона | `"+39"` (Италия) |
| `DEFAULT_CHECK_IN_HOUR` | Час заселения по умолчанию | `14` (14:00 / 2 PM) |
| `DEFAULT_CHECK_OUT_HOUR` | Час выселения по умолчанию | `10` (10:00 / 10 AM) |
| `DEFAULT_WIFI_NAME_PLACEHOLDER` | Плейсхолдер для названия Wi-Fi | `"hotel_wifi"` |
| `DEFAULT_WIFI_PASSWORD_PLACEHOLDER` | Плейсхолдер для пароля Wi-Fi | `"password_wifi"` |

## 📋 Доступные константы (Backend):

| Константа | Описание | Текущее значение |
|-----------|----------|-------------------|
| `DEFAULT_CHECK_IN_HOUR` | Час заселения по умолчанию (в entity Admin) | `14` |
| `DEFAULT_CHECK_OUT_HOUR` | Час выселения по умолчанию (в entity Admin) | `10` |

---

## 🔧 Как изменить значения по умолчанию

Просто отредактируйте значения в соответствующих файлах:
- **Frontend:** `admin-frontend/src/constants/defaults.ts`
- **Backend:** `hotel-backend/src/constants/defaults.ts`

⚠️ **Важно:** Если изменяете часы (CHECK_IN_HOUR/CHECK_OUT_HOUR), нужно изменить в **обоих** файлах (frontend и backend) для синхронизации.

---

## 📝 Примеры изменений:

### Изменить код страны по умолчанию:
```typescript
// admin-frontend/src/constants/defaults.ts
export const DEFAULT_PHONE_COUNTRY_CODE = "+380"; // Украина
```

### Изменить заглушку логотипа:
```typescript
// admin-frontend/src/constants/defaults.ts
export const DEFAULT_HOTEL_LOGO_URL = "https://example.com/my-default-logo.png";
```

### Изменить часы по умолчанию:
```typescript
// Frontend
// admin-frontend/src/constants/defaults.ts
export const DEFAULT_CHECK_IN_HOUR = 15;  // 15:00
export const DEFAULT_CHECK_OUT_HOUR = 11; // 11:00

// Backend
// hotel-backend/src/constants/defaults.ts
export const DEFAULT_CHECK_IN_HOUR = 15;  // 15:00
export const DEFAULT_CHECK_OUT_HOUR = 11; // 11:00

// Также нужно изменить в entity (для новых миграций):
// hotel-backend/src/entities/Admin.ts
@Column({ type: "int", nullable: true, default: 15 })
checkInHour!: number | null;

@Column({ type: "int", nullable: true, default: 11 })
checkOutHour!: number | null;
```

---

## 📌 Где используются константы:

### Frontend:
- ✅ `CreateHotelForm.vue` - форма создания отеля
- ✅ `EditHotelDialog.vue` - форма редактирования отеля
- ✅ `SaHotelsList.vue` - список отелей (заглушка логотипа)
- ✅ `SaHotelDetail.vue` - детали отеля (заглушка логотипа)

### Backend:
- ✅ `Admin.ts` entity - значения по умолчанию в БД
- ✅ `seedSuperadmin.ts` - начальные данные суперадмина

---

## ⚠️ Важные замечания:

1. **Синхронизация:** Часы по умолчанию должны совпадать в frontend и backend
2. **Миграции:** При изменении значений в entity Admin может потребоваться миграция БД
3. **Плейсхолдеры:** Значения плейсхолдеров в локализации можно оставить как есть (они только для отображения)

