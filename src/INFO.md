# Admin Frontend - Кореневі файли

## Призначення

Кореневі файли Vue додатку - точка входу та головний компонент.

## Структура

```
src/
├── main.ts          # Точка входу додатку
├── App.vue          # Головний компонент
├── style.css        # Глобальні стилі
├── vite-env.d.ts    # TypeScript типи для Vite
└── INFO.md          # Цей файл
```

## Файли

### main.ts

**Призначення:** Точка входу Vue додатку - ініціалізація всіх плагінів та монтування додатку.

**Що робить:**

1. **Створення додатку:**
   - `createApp(App)` - створює Vue додаток

2. **Підключення плагінів:**
   - `createPinia()` - state management
   - `router` - маршрутизація
   - `i18n` - інтернаціоналізація

3. **Монтування:**
   - `app.mount("#app")` - монтує додаток в DOM

**Порядок ініціалізації:**

1. Імпорт стилів (tailwind.css)
2. Створення app
3. Підключення плагінів
4. Монтування

### App.vue

**Призначення:** Головний компонент додатку - кореневий компонент, який містить загальну структуру.

**Що містить:**

- `<RouterView />` - відображення поточної сторінки
- `<GlobalLoader />` - глобальний індикатор завантаження
- `<NotificationContainer />` - контейнер для сповіщень

**Структура:**

```vue
<template>
  <div class="container">
    <RouterView />
    <GlobalLoader />
    <NotificationContainer />
  </div>
</template>
```

### style.css

**Призначення:** Глобальні CSS стилі додатку.

**Що містить:**

- Глобальні стилі
- CSS змінні
- Стилі для body, html

### vite-env.d.ts

**Призначення:** TypeScript типи для Vite змінних середовища.

**Що містить:**

- Типи для `import.meta.env`
- Типи для env змінних (VITE_API_URL, тощо)

## Структура проекту

```
src/
├── main.ts              # Точка входу
├── App.vue              # Головний компонент
├── api/                 # HTTP клієнт та API виклики
├── assets/              # Статичні ресурси
├── components/          # Vue компоненти
├── composables/         # Перевикористовувана логіка
├── constants/           # Константи
├── i18n/                # Інтернаціоналізація
├── layouts/             # Макети сторінок
├── router/              # Маршрутизація
├── stores/              # Pinia stores
├── types/               # TypeScript типи
├── utils/               # Допоміжні функції
└── views/               # Сторінки додатку
```

## Запуск

### Development

```bash
npm run dev
```

### Production Build

```bash
npm run build
```

## Важливі моменти

- **Порядок ініціалізації** - плагіни підключаються в правильному порядку
- **Глобальні компоненти** - GlobalLoader та NotificationContainer завжди доступні
- **RouterView** - відображає поточну сторінку на основі маршруту

## 🔮 Майбутній функціонал

### 🤖 AI-асистент для адміна

**Призначення:**
AI-асистент для адміністраторів з інтеграцією з AI-модулем backend.

**Структура компонентів:**
- `/components/ai/AiChat.vue` — чат-інтерфейс для AI
- `/components/ai/AiSuggestions.vue` — пропозиції AI
- `/stores/ai.ts` — Pinia store для AI стану

**Функціональність:**
- Швидкі відповіді по проживаннях, кімнатах, гостях
- Пошук та фільтрація через AI
- Автоматичне формулювання відповідей
- Підтримка мов: uk/en/it/de

**API інтеграція:**
- `POST /admin-ai/chat` — відправка повідомлень до AI
- Обробка контексту (поточний stay, room, hotel)
- Відображення історії чату

### 📅 Календар бронювання номерів

**Призначення:**
Візуальний календар зайнятості номерів для адміністраторів та редакторів.

**Компоненти:**
- `/components/calendar/RoomsCalendar.vue` — головний календар зайнятості
- `/components/calendar/CalendarView.vue` — різні види календаря (місяць/тиждень/день)
- `/components/calendar/RoomTimeline.vue` — таймлайн для конкретного номера
- `/components/calendar/BookingCard.vue` — картка бронювання на календарі
- `/components/calendar/CalendarFilters.vue` — фільтри календаря

**Функціональність:**
- Наглядний календар з усіма номерами та їх зайнятістю
- Кольорове кодування статусів (booked, occupied, cleaning, free)
- Перегляд по місяцях/тижнях/днях
- Drag & drop для зміни дат бронювань
- Швидке створення бронювання з календаря
- Фільтри: по номерах, статусах, датах
- Пошук по гостях
- Відображення конфліктів дат
- Експорт календаря (PDF, Excel)

**API інтеграція:**
- `GET /rooms/calendar` — отримання даних календаря
- `GET /rooms/calendar/:roomNumber` — календар конкретного номера
- Обробка real-time оновлень

## 📅 Останні зміни (2025-01-26)

### ✅ Додано:
- **Wi-Fi параметри в суперадміні:**
  - Відображення Wi-Fi параметрів в інформації про готель (SaHotelDetail)
  - Редагування Wi-Fi параметрів в модалці редагування отеля (EditHotelDialog)
  - Поля Wi-Fi в формі створення готелю (CreateHotelForm)
- **Счетчики для часу:**
  - Кнопки +/- для check-in/check-out часу в EditHotelDialog
  - Кнопки +/- для check-in/check-out часу в CreateHotelForm
- **Модалка створення проживання:**
  - CreateStayModal з деталями гостей (firstName, lastName, email, phone, guestsCount)
  - Счетчик для guestsCount з обмеженням по місткості номера
  - Повна валідація та локалізація
- **Логування для відлагодження:**
  - Логування процесу збереження в EditHotelDialog
  - Логування даних з БД в SaHotelDetail

### 🔧 Виправлено:
- Кнопка збереження в EditHotelDialog тепер всередині форми
- Завантаження Wi-Fi даних з БД (виправлено функцію getUsers)
- Обробка помилок (заміна `any` на `unknown` з правильними перевірками)
- Відображення помилок блокировки користувачів (специфічні повідомлення)

### 🛡️ Безпека:
- Маскування чутливих даних в логах (паролі, токени)
- Обмеження логування помилок аутентифікації
- Перевірка блокировки користувачів при логіні

---

**Останнє оновлення:** 2025-01-26

